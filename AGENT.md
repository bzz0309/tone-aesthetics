# AGENT.md

## 项目目标

维护一套由 `SKILL.md` 调度、人物规范与造型库分工协作的品牌广告系统，并通过实际人物图验证人物结构、穿搭、投放构图和视频身份连续性。

## 开始工作前

1. 阅读 `README.md`、`worklog.md`、`methods.md` 和 `handoff.md`。
2. 若任务通过安装后的 skill 进入，先阅读根目录 `SKILL.md`；将其视为执行入口，不视为人物或造型规范源。
3. 将 `outputs/taste.md` 视为人物身份、人设与广告世界规范源，将 `outputs/styling-library.md` 视为造型路线、具体 Look 与匹配规则源。
4. 检查现有改动与生成记录，不覆盖用户已经确认的文件。

## 人物与图像规则

- 默认图片生成模型为 GPT Image 2，项目逻辑名称写作 `gpt-image2`；模型不可用、调用失败或环境只提供其他模型时允许自动切换／降级。每轮必须记录请求模型、实际模型与回退原因，不得把替代结果误标成 GPT Image 2。
- 只创作原创虚构人物；真人或明星参考只能拆解为通用结构语言，不追求相似度。
- 默认人物为 18–30 岁亚洲成年人，禁止幼态、网红模板、偶像模板和非用户要求的混血化。
- 女性 F01–F05 与男性 M01–M05 是两套独立路线，不做直接性别转换。
- 女性造型使用 WST01–WST10，男性造型使用 MST01–MST06；先按性别进入造型池，不跨性别复制整套 Look。
- 常规广告先从女性 WBASE01–WBASE07 或男性 MBASE01–MBASE04 选择基础衣橱，再叠加一条造型路线；强概念广告才使用 DIRECT。
- 高辨识度必须来自至少三个结构轴，发型、妆容、服装、首饰和灯光不计入身份差异。
- 人物进入全身、动作或视频资产前，先确认正面身份图，并锁定脸型、眼距、鼻唇、发际线和年龄。
- 区分“身份测试”和“广告角色”：身份测试可以使用中性棚拍；最终广告角色必须包含人设、妆造、英雄道具、真实动作和匹配场景。
- 参考图优先提炼组合机制，不复刻具体脸、服装、人数、构图、品牌 Logo 或字幕；染发、眼镜、朋克、Y2K 等都不得成为全角色默认模板。
- 广告妆造必须明确发型、发色、妆容重点、一个主造型信号、最多三个配饰区域、一个英雄道具；道具必须被真实使用。
- 发色优先服从人物路线：F02 中性先锋、F05 灵动甜酷可以主动使用明显染色；F01 清冷极简、F03 自然淡颜、F04 摩登浓颜以黑色、棕色及低明度色域为主。广告主题不能越过人物气质强行漂染。
- 场景必须由角色行为反推，至少提供三个空间证据，并让时间、天气、光线、风与色彩共同形成品牌氛围；普通巷道不得作为音乐、运动或职业角色的万能背景。
- 生成图必须归档最终图、实际提示词和 QC；失败或被否决的图不得标记为最终角色。
- 不在人物图中生成第三方 Logo、未经授权文字或难以确认来源的品牌图形。
- `outputs/reference_*` 中的研究素材不得由 skill 自动加载为生成输入或对外资产；公开仓库中的素材必须单独完成权利审查。

## 文件约定

- 新一轮人物测试使用 `outputs/casting-round-NN-description/`。
- 每轮至少保留 `prompt_used.md`、`qc_report.md` 和最终候选图。
- 不覆盖历史图，使用 `v2`、`v3` 等版本名。
- 对 `taste.md` 或 `styling-library.md` 的修改必须同步更新 `worklog.md`；形成可复用方法时同步更新 `methods.md`。

## 验证要求

- Markdown 代码围栏必须成对。
- `taste.md` 中的本地链接必须存在。
- 全身图必须完整显示头发至鞋底，手脚、服装和配饰结构可用。
- 广告角色图必须在三秒内说明“是谁、在哪里、正在做什么、为什么适合该产品”；道具必须被真实使用。
- 连续视频素材必须检查身份、发型、服装、配饰和动作因果连续性。

## 发布安全

- GitHub 默认使用 private 仓库，除非用户明确要求公开。
- 参考图仅供内部研究；转为 public 前必须单独审查并移除无权公开的素材。
- 禁止提交密钥、令牌、账户信息或包含个人隐私的文件。
